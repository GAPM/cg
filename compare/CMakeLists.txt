CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(compare)

INCLUDE_DIRECTORIES(../bitcpp)

ADD_EXECUTABLE(compare main.cpp)
SET_PROPERTY(TARGET compare PROPERTY CXX_STANDARD_REQUIRED ON)
SET_PROPERTY(TARGET compare PROPERTY CXX_STANDARD 14)

TARGET_LINK_LIBRARIES(compare bitcpp)
